services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: toastyleague
      POSTGRES_PASSWORD: toastypw
      POSTGRES_DB: toastyleague
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
